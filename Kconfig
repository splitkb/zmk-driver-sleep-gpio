menuconfig SPLITKB_SLEEP_GPIO
    bool "Sleep GPIO support"
    default $(dt_compat_enabled,$(DT_COMPAT_SPLITKB_SLEEP_GPIO))
    depends on ZMK_SLEEP
    help
        Enable GPIO control based on ZMK sleep state